node_modules/
dist/
.wrangler/
.dev.vars
.env
.env.*
*.local
.DS_Store
coverage/

# Скриншоты сквозных прогонов — отладочный след, не эталон.
# Прогон кладёт снимок в свою рабочую директорию: из `run-all.sh` это `tests/`,
# при запуске поштучно — корень.
tests/*.png
/*.png

# Пример окружения мини-аппа — единственный .env-файл в репозитории.
!apps/web/.env.example
