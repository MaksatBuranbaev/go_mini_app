{
  "name": "@go/room",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@go/engine": "workspace:*",
    "@go/protocol": "workspace:*",
    "@telegram-apps/init-data-node": "^2.0.9"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.0.5",
    "wrangler": "^4.42.0"
  }
}
